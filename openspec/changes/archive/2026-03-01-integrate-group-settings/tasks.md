## 1. Setup

- [x] 1.1 Create the `group-settings` directory under `app/badminton/`
- [x] 1.2 Add a basic `page.tsx` within the new directory to establish the route

## 2. Core UI Implementation

- [x] 2.1 Retrieve the generated front-end code/components from the Stitch project "Exercise Group Settings"
- [x] 2.2 Implement the main layout of the group settings page inside `app/badminton/group-settings/page.tsx`
- [x] 2.3 Add necessary local state hooks for toggles and settings options
- [x] 2.4 Implement the inner components (toggles, settings list, header) as defined in the mockup

## 3. Polish and Review

- [x] 3.1 Implement a button or interaction to export the state as JSON to the clipboard
- [x] 3.2 Verify styling and responsiveness, especially on mobile views (theme: Dark Mode, Lexend font, accent color #13ec5b)
- [x] 3.3 Add navigation link from the main badminton page to the new group settings page
